<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Dhun | Home</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css">
        <link rel="stylesheet" href="/assets/css/style.css">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
    </head>

<body>
    <section class="double-section">
        <div class="double-container" style="height: 100vh;">
            <%- include('partials/left-container'); %>
            <div class="right-container">
        <%- include('partials/side-header'); %>
        <p style="color: white;"><%= message%></p> 
        <form action="/upload-song" method="post" enctype="multipart/form-data" style="display: grid; justify-content: center; grid-gap: 10px; transform: translateX(-150px); margin-top: 55px;">
            <input type="text" name="title" placeholder="Song Title" style="padding-left: 10px; width: auto; height: 45px; outline: none; border: none; border-radius: 8px;"><br/>
            <label for="song" id="songLabel" style="color: white; margin-top: 10px;">Song:</label>
            <input id="song" type="file" name="song" style="padding-left: 10px; width: auto; height: 45px; outline: none; color: white; border: none; border-radius: 8px;" onchange="changeLabelColor('songLabel')"><br/>
            <label for="poster" id="posterLabel" style="color: white; margin-top: 10px;">Poster: </label>
            <input id="poster" type="file" name="poster" style="padding-left: 10px; width: auto; height: 45px; color: white; outline: none; border: none; border-radius: 8px;" onchange="changeLabelColor('posterLabel')"><br/>
        
            <label for="category" style="color: white; margin-top: 10px;">Category: </label>
            <select name="category" id="category" style="padding-left: 10px; width: auto; height: 45px; outline: none; border: none; border-radius: 8px;">
                <% allCategory.forEach(function(item) { %>
                    <option value="<%=item.id%>"> <%=item.name%></option>
                    <% }); %>
            </select>
            <button type="submit" style="margin-top: 10px; width: auto; height: 45px; background-color: red; outline: none; border: none; border-radius: 2px;">Upload</button>
        </form>
        </div>
    </div>
    </section>
</body>
</html>